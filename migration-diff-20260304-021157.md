# Migration Diff Report
Generated: 2026-03-04 02:11:57

## `.worktrees\plan4-watch-computed\tests\fixtures\dummy_project\src\composables\useDashboard.js`

**New file**

```javascript
// Migration confidence: LOW (5 warnings — see migration report)
import { ref, computed, onBeforeUnmount, nextTick } from 'vue'

export function useDashboard() {
  const stats = ref([])
  const error = ref(null)
  const lastRefresh = ref(null)

  // ⚠ MIGRATION: this.$route is not available in composables
  const currentPage = computed(() => { return this.$route.params.page || 1 })
  const totalCount = computed(() => { return stats.value.length })

  async function loadStats() {
    error.value = null
    this.isLoading = true
    try {
      const page = this.$route.params.page
      // ⚠ MIGRATION: this.$store is not available in composables
      const data = await this.$store.dispatch('fetchStats', { page })
      stats.value = data
      lastRefresh.value = Date.now()
    } catch (e) {
      error.value = e.message
      // ⚠ MIGRATION: this.$emit is not available in composables
      this.$emit('stats-error', e)
    }
  }
  function refresh() {
    nextTick(() => {
      loadStats()
    })
  }

  loadStats()

  onBeforeUnmount(() => {
    // ⚠ MIGRATION: this.$off is removed in Vue 3
    this.$off('stats-updated')
  })

  return { stats, error, lastRefresh, currentPage, totalCount, loadStats, refresh }
}
```

## `tests\fixtures\dummy_project\src\components\Dashboard.vue`

```diff
--- a/tests\fixtures\dummy_project\src\components\Dashboard.vue
+++ b/tests\fixtures\dummy_project\src\components\Dashboard.vue
@@ -12,11 +12,20 @@
 </template>
 
 <script>
-import { dashboardMixin } from '@/mixins/dashboardMixin'
 
+import { onBeforeUnmount } from 'vue'
+import { useDashboard } from '@/composables/useDashboard'
 export default {
   name: 'Dashboard',
-  mixins: [dashboardMixin],
+  setup() {
+    const { stats, error, lastRefresh, currentPage, totalCount, refresh, loadStats } = useDashboard()
+    loadStats()
+    onBeforeUnmount(() => {
+        this.$off('stats-updated')
+    })
+
+    return { stats, error, lastRefresh, currentPage, totalCount, refresh, loadStats }
+  },
   data() {
     return {
       isLoading: false,
```
